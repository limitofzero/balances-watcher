apiVersion: apps/v1
kind: Deployment
metadata:
  name: token-balances-watcher
  labels:
    app: token-balances-watcher
spec:
  replicas: 1
  selector:
    matchLabels:
      app: token-balances-watcher
  template:
    metadata:
      labels:
        app: token-balances-watcher
    spec:
      containers:
        - name: app
          image: ghcr.io/limitofzero/balances-watcher:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          envFrom:
            - secretRef:
                name: token-balances-watcher-secrets
          env:
            - name: RUST_LOG
              value: "${RUST_LOG:-info}"
            - name: HTTP_BIND
              value: "${HTTP_BIND}"
