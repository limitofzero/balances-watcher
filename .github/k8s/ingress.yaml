apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: balances-watcher
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: balances-watcher
                port:
                  number: 80